#include<iostream>
using namespace std;

int ispalindrom(int num, int *temp){
// base case
if(num>=0 and num <= 9 ){
    return (num ==(*temp)%10)
}
bool return = (f(num%10, temp) and (num%10) == (*temp) % 10);
(*temp) /= 10;
return result;
}

int main(){
   int num = 12343;
   
   cout<<f()

    return 0;
}