#include<iostream>

using namespace std;

inst amin(){

// user inpiut 

int n;
cout<<" inter the value of n :"<<endl;
cin>>n;

//output
cout<<" the value of n"<<n<<endl;

//if else condition singal condition check
if(condition true){
    // execute this code
}else{
    // other wise run this code
}

// multiple condition are check
if(condition 1){

}else if(conditiom 2){

}else if(condition 3){

}else{
    // condition are not true;
}

// switch statement are the check of one statement to multiple condition 

switch(condition are check){
case: 
    break;

 case:
    break;

case:
    break;
 default:
    break;
}

for(int i=0; i<n; i++){
    // write the code here
}

/* while loop are the use the condition are true run the
 code multiple time when condtion are not true */
while(n> 4){
    // write a code here
}

do
{
    // code is one time is run condition are true or false
} while (/* condition */);


// breack are used to stop the code condion are matched

/* continue are used to skip the one condion and
 run again and again the condition are not execute */

 // comment are used the describ the code
 // comments are two tipes
 //-- this is colled singal line comments
 // /**/ -- this is colled multiple line comments

 // function are the buccket off condion and statement this is write a special task
 // void are not return any value 
 // paranthises ()- are the passed of multipal parameters ||arguments ||are guments are two tips pass by value and pass by reference
 void addTwoNumber( ){
  // inside the function are write multiple condion and expreseion
 }

 // recursion are used the coptimizartion are the code time and space complecity
 // using the recursion are devided the bigger probles to multiple small sub problems 
 // base case 
 // assumtion 
 // selfwork

 // array is sequence  container to store the similar types of value
 int arr[10];// deceleare the 10 size of array
 int arr[10] = [1,2,3,4,5,6,6,7,8,9,10] // initailize the array

 int sizeof()// are the give thi size of array

//

    return 0;
}
